export interface Page {
  id: number;
  title: string;
  url: string;
}

export interface NewPage {
  title: string;
  url: string;
}

export interface PageDTO {
  id: number;
  title: string;
  url: string;
}

export interface NewPageDTO {
  title: string;
  url: string;
}
